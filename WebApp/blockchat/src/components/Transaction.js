import React from 'react';
import '../global.css';

const Transaction = () => {
    return (
        <div className="Transaction">
            <h1>Make a new Transaction</h1>
            <p>You can transfer coins or send a message!</p>
        </div>
    );
}

export default Transaction;
